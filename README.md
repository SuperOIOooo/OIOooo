# OIOooo
自己用 也欢迎大家用 （www
这里是잉ㅇㅇㅇ
瞎写了个PS辅助批量导出PNG脚本
因为找不到已有的好用脚本（菜
所以我自己写了一个
希望能帮助到一些人吧（www 


使用步骤：
1. 菜单：文件 → 脚本 → PSD-Batch-Export-PNG。
2. 在弹出的窗口中：
   - “源文件夹”点“浏览”选择存放 PSD 的文件夹；
   - 默认勾选“输出到源文件夹下的 png 子目录”，取消勾选后可自选输出文件夹；
   - 可选“包含子文件夹”；
   - 点“开始导出”，界面会显示进度条和当前文件名，结束后弹出成功/失败统计。
